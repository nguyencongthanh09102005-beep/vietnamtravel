name: Refactor and build

on:
  push:
    branches:
      - refactor/portfolio-cleanup
  pull_request:
    branches:
      - main

concurrency:
  group: vietnam-travel-refactor-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  migrate-and-build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Refactor province details
        run: node scripts/refactor-province-details.mjs

      - name: Remove unused generated UI kit
        run: rm -rf src/app/components/ui

      - name: Validate referenced assets
        run: node scripts/check-assets.mjs

      - name: Install dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Build production bundle
        run: npm run build

      - name: Commit generated cleanup
        if: github.event_name == 'push'
        run: |
          if git diff --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: generate clean province data and remove unused UI kit [skip ci]"
          git push
